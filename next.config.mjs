// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No pongas `output: 'export'` porque rompes /api
  // Nada de experimental.appDir
};

export default nextConfig;
